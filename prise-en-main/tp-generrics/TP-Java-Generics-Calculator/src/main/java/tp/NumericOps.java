package tp;

/**
 * NumericOps<T> encapsule les opérations élémentaires pour un type numérique T.
 * On implémentera une version par type (Integer, BigDecimal, etc.).
 *
 * TODO:
 *  - Déclarer add, sub, mul, div
 */
public interface NumericOps<T> {
 
}
